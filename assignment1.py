import pandas as pd

customers_df = pd.read_csv("customers.csv")
"""first_name/ last_name is not .strip(), email columns missing, join_date very different formats and some Years are in city, city column is misaligned with some countries in the the column on the right of city """


